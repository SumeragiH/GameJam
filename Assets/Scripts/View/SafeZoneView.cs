using UnityEngine;
using System.Collections;

/// <summary>
/// 默认圆形Sprite, 半径可以自己设置
/// 自带一个CoverView
/// </summary>
class SafeZoneView : MonoBehaviour
{
    [SerializeField] private CoverView _coverView;
}